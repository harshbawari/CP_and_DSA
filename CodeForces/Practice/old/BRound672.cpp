#include <bits/stdc++.h>

using namespace std;

#define ll long int

void solve() {
    int n, count = 0;

    cin >> n;
    vector<ll> a(n);

    for(int i = 0; i < n; i++)
        cin>>a[i];
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);

    int t;

    cin>>t;

    while(t--) {
        solve();
    }
}